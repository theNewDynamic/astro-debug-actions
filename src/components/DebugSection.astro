---
interface Props {
  title: string;
}
const { title } = Astro.props;
---
<div class="flex flex-col gap-2">
  <div class="border-b border-white/20 pb-1 text-[0.65rem] uppercase tracking-wide text-white/50">{title}</div>
  <slot />
</div>
